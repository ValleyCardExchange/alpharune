# SDD ledger — plan: docs/superpowers/plans/2026-09-07-kennen-tyler-deck.md

Spec: docs/superpowers/specs/2026-09-07-kennen-tyler-deck-design.md (binding; addendum overrides inline text).
Branch: kennen-tyler-deck (container-local until Tyler's GitHub fork exists). Plan commit: 2ad0dc8.
Controller rule (personal-ai CLAUDE.md): subagent reports are claims — controller re-runs the decisive check (tests / grep / git status) before ledgering a completion.

## Pre-flight conflict scan (2026-09-07)

| Pair / task | Produces vs consumes | Finding | Ruling |
|---|---|---|---|
| T1 ↔ T2 | `ActivationCost::disempower_self`, `empowerObject/disempowerObject` decls | consistent | — |
| T1 ↔ T3 | `CardPlayedEvent::play_source`, `playSourceFor`, `WhenYouPlayFromNonHand` | consistent | — |
| T1 ↔ T4/T5 | `Intent::flow_source`, `ChainItem::banish_on_leave`, `liveFlowCosts`, `Card::FlowCost` | consistent | — |
| T1 ↔ T5 | T1 says the `card_helpers.h` banish branch "lands here but stays unexercised"; T5 says "implement if left as stubs" — and T1 would put behaviour in before test #10 exists (TDD violation) | conflict | **Ruling:** T1 does NOT touch `card_helpers.h`; T5 edits it after test #10 is red. Cost if wrong: one extra partial rebuild of the card files that include the header (~10–15 min). |
| T1 ↔ T6/T7 | `burnCards`, `burnOut`, `revealAndChoose(…, RestDestination)` decls; existing 2-arg callers must still compile (default `Recycle`) | consistent | — |
| T1 ↔ T9 | `transient_power_discount`, `friendly_spell_power_discount`, `target_battlefield_restriction` | consistent | — |
| T8 ↔ T9 | T8 Step 2 lists test #22 (Tomb flag) AND T9 Step 1 lists #22; T8 would create the Tomb card file with no effect code → coverage gate flags a stub → T8 Step 5 fails | conflict | **Ruling:** the Sandswept Tomb card file (0792) moves entirely to T9 (T9 Step 1 creates it red-then-green with #22); T8 authors four cards. Deck-validator test #26 needs the Tomb to exist → #26 moves to the end of T9. Cost if wrong: none material — ordering only. |
| T1 internal | test #27's RED is a compile error ("no member named Flow") rather than an assertion failure | acceptable — fails for the expected reason; implementer records the compiler line | — |
| T1 internal | executor/engine bodies left as empty stubs after T1 | acceptable — later tasks' tests go red against the stubs, which is a genuine RED | — |
| T2 internal | test #3 uses a test-local legend registered in the fixture's registry | self-consistent | — |
| T3 internal | Step 6 revert/restore technique to prove #5 red | self-consistent | — |
| T4 internal | Step 10 proves #12 red by temporarily disabling one generator | self-consistent | — |
| T8 internal | Step 2's list "#15–#21, #17, #28" repeats #17 — harmless | wording | — |

## Task log
- Task 1: implementer DONE_WITH_CONCERNS (commit a040093; controller re-ran suite: 1058 passed, 1 pre-existing disabled; pushed to fork). Reviewer dispatched on review-2ad0dc8..a040093.diff.
- Task 1: Ruling: implementer extended `Intent::operator==` with `flow_source` + `target_battlefield_restriction` (not on the brief's list) — KEEP: spec §3/§4 emit twin intents differing only in those fields, and a partial comparison would conflate them in the agent's chosen_idx path. Cost if wrong: one-line revert; zero behaviour change today.
- Task 1: minor (deferred): pre-existing `Intent::operator==` omits `use_alt_play_cost` and `granted_ability_def` (same conflation hazard; not this plan's) — for the final review to triage.
- Task 1: note for Task 3 dispatch: `playSourceFor` is an unconditional-Hand stub; any WhenYouPlayFromNonHand test written before Task 3 replaces it passes for the wrong reason — Task 3's RED must be observed against the stub.
- Task 1: reviewer ✅ spec / Approved (0 critical, 0 important; minors: FlowCost duplicates AltPlayCost shape — plan-mandated name; playSourceFor stub reads as finished — carried in the Task 3 note). ⚠️ trailers verified by controller via git log: present.
- Task 1: complete (commits 2ad0dc8..a040093, review clean)
- Task 2: implementer DONE_WITH_CONCERNS (commit cbcadbf; controller re-ran suite: 1062 passed; pushed to fork). Concerns: (a) added `GameEngine::testHook_initSubsystems()` in game_engine.h (outside the brief's file list) because a bare GameEngine has null subsystems outside runGame/resume; (b) `is_empowered` clear-on-leaving-board (spec §1) not implemented. Reviewer dispatched on review-a040093..cbcadbf.diff with both as named risks.
- Task 2: reviewer ❌ scope / Needs fixes — Important #1: testHook_initSubsystems triplicates the subsystem-init block (runGame, resumeFromSnapshot, hook) → factor a private initSubsystems(). ⚠️: spec §1 clear-on-board-exit unimplemented anywhere.
- Task 2: Ruling: clear-on-board-exit IS binding (spec §1); the spec's parenthetical "(same places is_stunned is reset)" was WRONG — stun decays per turn at EndingStep, not on board exit. Implement the clear in killObject/bounceToHand/banishObject (+recycle if it takes objects off the board), with tests. Spec amended as addendum #10. Cost if wrong: three one-line assignments and two tests; in-scope only the legend is ever empowered and never leaves the board, so no gameplay impact either way.
- Task 2: fix round 1/5 dispatched (2 findings) — resumed original implementer.
- Task 2: fix round 1/5 (2 addressed per implementer, 1 residual self-reported: GameEngine::killUnit combat-kill path still doesn't clear Empowered; commits cbcadbf..138527e; controller re-ran suite: 1064 passed). Re-review deferred: round 2 dispatched immediately for the killUnit path; ONE scoped re-review will cover cbcadbf..HEAD.
- Task 2: fix round 2/5 (killUnit path; commit 3616d66; controller re-ran suite: 1065 passed; pushed). Scoped re-review dispatched over cbcadbf..3616d66 (covers rounds 1+2).
- Task 2: re-review (rounds 1+2): both findings ADDRESSED, no new breakage.
- Task 2: Ruling (on the re-reviewer's out-of-scope sweep): further board-exit sites that do not clear Empowered — battlefield replace/swap-back (effect_executor.cpp:~917, game_engine.cpp:~5849/~5866), the inline recycle-self activation-cost payment (game_engine.cpp:~1198), rune cost-payment recycles, the "kill this instead" replacement trash (~5641), hidden-card destroy on controller mismatch (~4475) — PARKED as deferred minors for the final review: no card in either deck can empower a battlefield, a rune, or a unit with a recycle-self ability, and the legend never leaves the board; the generic-field hazard is real but inert in scope. Cost if wrong: a few one-line resets at named sites.
- Task 2: minor (deferred): `src/core/game_object.h:~39-42` field comment on is_empowered still carries the superseded "(same places is_stunned is reset)" wording — update in the final fix wave (Task 1 file; a header touch costs a rebuild).
- Task 2: complete (commits a040093..3616d66, review clean after 2 fix rounds)
- Task 3: implementer dispatched (sonnet) at HEAD 3616d66; carried the stub-RED note and addendum #2 mapping.
- Task 3: implementer DONE (commit ce001f6; controller re-ran suite: 1068 passed; pushed). Note: touched intent.h for the shared zone→source mapping (one more full rebuild). Reviewer dispatched.
- Task 3: reviewer ❌ / Needs fixes — Important: the LIVE hidden-reveal path is ChainManager::stepExecuteAndPass (chain_manager.cpp:~229-266), which emits CardPlayedEvent without play_source; the two game_engine.cpp facedown sites the brief named (executePlayFromHidden) are dead code. Plan's "five emit sites" was wrong: six.
- Task 3: Ruling: widen Task 3's file scope to src/engine/chain_manager.cpp and fix the live path test-first (hidden reveal → play_source Hidden, legend trigger fires). Dead executePlayFromHidden left in place this round (deletion is unrelated cleanup). Cost if wrong: none — the fix is one capture + one event field. Plan text "five sites" superseded by this entry.
- Task 3: minor (deferred → folded into round 1): rename zone-derived locals to event_play_source; comment the redundant captures.
- Task 3: observation for the final review: PlayedFromFacedownEvent (Katarina) is also emitted only from the dead executePlayFromHidden — pre-existing gap, not this plan's.
- Task 3: fix round 1/5 dispatched — resumed original implementer.
- Task 3: fix round 1/5 (commit 37fddf4; controller re-ran suite: 1069 passed; pushed). Scoped re-review dispatched over ce001f6..37fddf4.
- Task 3: re-review round 1: all findings ADDRESSED, no new breakage. Observation (non-blocking): the hidden-reveal test hand-mirrors resolveSpell's dispatch; a shared "manually-driven chain" helper could be extracted if the pattern recurs.
- Task 3: complete (commits 3616d66..37fddf4, review clean after 1 fix round)
- Task 4: implementer dispatched (opus — core Flow subsystem) at HEAD 37fddf4; dispatch corrected the plan's call-site count (three trash-replay sites) and use_flow_cost→flow_source.
- Task 4: implementer DONE (commit aa9f080; controller re-ran suite from repo root with RIFTBOUND_ROOT=.: see line below; pushed). Reviewer dispatched.
- Task 4: reviewer ✅ requirements / Needs fixes — Important #1: flow execution branch fails OPEN (no offer match → printed cost from trash; grant reset outside loop; payAdditionalCost bool discarded; no Trash zone check in liveFlowCosts). Minors: third copy of timing gate; by-id vs back() chain lookup; coverage gaps (showdown site, cant_play suppression, printed-while-granted); OpenSpiel vocab collision (pre-existing shape); report parity wording.
- Task 4: Ruling: fail LOUDLY — validate before mutation, reject with a FLOW: log line; payment must succeed before paid_via_flow; grant consumed only by the GRANTED offer (Minor #3 folded); Trash zone check in liveFlowCosts. Cost if wrong: an illegal intent that can't be generated today is rejected instead of played — no gameplay cost.
- Task 4: Ruling: fold Minors #2 (shared gate helper for the two byte-identical generators), #5 (one by-id lookup) and #6 (three coverage tests) into round 1.
- Task 4: parked — OpenSpiel action_vocab hashes Play intents by card def only, so printed/granted/replay offers for one def collide in the policy head (pre-existing shape; addendum #1 "one intent per live offer" is engine-complete but not vocab-expressible) — Ruling: out of this plan's scope; for the final review / a follow-up. Cost if wrong: the MCTS/ML agent cannot distinguish which Flow cost it picks (random tie-break), which understates Kennen's grant value in self-play.
- Task 4: fix round 1/5 dispatched — resumed original implementer.
- Task 4: fix round 1/5 (4 addressed per implementer; commit 2b3a27a; controller re-ran suite: 1080 passed; pushed). Scoped re-review dispatched over aa9f080..2b3a27a.
- Task 4: re-review round 1: all 4 findings ADDRESSED, no new breakage (logWarn confirmed visible by default; helper decision-equivalent in all four turn states).
- Task 4: complete (commits 37fddf4..2b3a27a, review clean after 1 fix round)
- Task 5: implementer dispatched (sonnet) at HEAD 2b3a27a. Ruling: counter disposal is scattered (counterChainTop + self-disposing counter cards e.g. Defy, Hard Bargain) → consolidate into ONE shared helper in card_helpers.h that branches on banish_on_leave; card files that self-dispose are in scope. Cost if wrong: a partial rebuild of the card files that include card_helpers.h.
- Task 5: implementer DONE (commit d37ace7; controller re-ran suite: 1084 passed; pushed). Open judgment: Abandon (693) returns a countered spell to hand and was left untouched. Reviewer dispatched with the CR 829.1.b.1 text as a constraint.
- Task 5: reviewer ❌ / Needs fixes — Critical: Flurry of Feathers (606) self-disposes to trash, unrouted; Important: Abandon (693) exclusion unresolved; inventory not trustworthy → second pass.
- Task 5: Ruling: Abandon — CR 829.1.b.1 replaces the leave-the-chain event itself, so a Flow-played spell countered by Abandon is BANISHED, not returned to hand; non-flow spells keep Abandon's hand-return. Cost if wrong: one card outside both decks behaves per a stricter CR reading; a one-line revert.
- Task 5: fix round 1/5 dispatched (Flurry routing + test; Abandon per ruling + tests; exhaustive inventory) — resumed original implementer.
- Task 5: fix round 1/5 (commit c493501; controller re-ran suite: 1087 passed; pushed). Scoped re-review dispatched over d37ace7..c493501.
- Task 5: re-review round 1: all 3 findings ADDRESSED (inventory independently confirmed exhaustive: 9 counter cards + Counter Strike false positive), no new breakage.
- Task 5: complete (commits 2b3a27a..c493501, review clean after 1 fix round)
- Tasks 6+7: batched into ONE dispatch (both small executor edits). Ruling: burn-out exists in BOTH EffectExecutor::drawCards and GameEngine::drawCards; EffectExecutor::burnOut becomes the single copy — executor drawCards and burnCards call it directly; GameEngine::drawCards delegates to effect_executor_->burnOut (implementer verifies initSubsystems precedes every GameEngine::drawCards caller, incl. the opening-hand draws at ~:444). Cost if wrong: a null-executor crash on a draw path — guarded by an assert + the existing burn-out tests.
- Tasks 6+7: implementer DONE (commits ba96b59, 06ab7c1; controller re-ran suite: 1091 passed; pushed). Concern: a 'same-class fallback' in GameEngine::drawCards for a null executor (test fixture bypasses initSubsystems). Reviewer dispatched.
- Tasks 6+7: reviewer ✅ spec / Approved on substance; Important: the null-executor fallback in GameEngine::drawCards is a production branch covering a test-fixture bug (fixture never calls testHook_initSubsystems) with a dead-in-Release assert. Ruling: fix the fixture (scope widened to tests/cards/test_burn_out_and_scoring.cpp) and DELETE the fallback. Cost if wrong: none — the fallback was reachable only from that fixture.
- Tasks 6+7: minor (deferred): burnCards's early-return-on-victory branch untested (optional test offered in round 1).
- Tasks 6+7: observation for final review: Minefield (0526) "top 2 to trash" is Burn 2 — could use burnCards.
- Tasks 6+7: fix round 1/5 dispatched — resumed original implementer.
- Tasks 6+7: fix round 1/5 (commit 5e10eb0; controller re-ran suite: 1092 passed; fallback gone by grep; pushed). Scoped re-review dispatched over 06ab7c1..5e10eb0.
- Tasks 6+7: re-review round 1: finding ADDRESSED (fallback deleted, fixture initialises subsystems, optional test added), no new breakage.
- Tasks 6+7: complete (commits c493501..5e10eb0, review clean after 1 fix round)
- Task 8: implementer dispatched (sonnet) at HEAD 5e10eb0. Per pre-flight ruling: FOUR cards (788–791); Sandswept Tomb (792) and deck-validator test #26 move to Task 9.
- Task 8: implementer DONE (commits 0785168..2049853; controller re-ran suite: 1101 passed; coverage 791 total / INCOMPLETE 0; no APPROX in the four card files; pushed). Reviewer dispatched.
- Task 8: reviewer ❌ / Needs fixes — Critical: Lightning Rush via revealAndChoose can draw all three (helper has no cap) — a plan-mandated defect (spec §6 named the helper); Important: "look at" modelled as a public Reveal; test #28 vacuous (no chain drain); test #17 relies on unordered_map order. Minors: expiry asserts keyword only; def-less objects would get a 0/0 grant; no test ties the real Flow cards to the trash offer.
- Task 8: Ruling: Lightning Rush implements its own resumable look-and-choose (Stacked Deck pattern + a decline mode) with draw bookkeeping done in the card — NO executor edit (avoids a header change/rebuild); `revealAndChoose(…, Trash)` from Task 7 stays as a tested helper with no in-scope caller. Spec §6 + addendum #4 superseded on this point → addendum #11 to be written. Cost if wrong: one unused executor branch; the card behaviour is exactly the printed text either way.
- Task 8: Ruling: private look = CardRevealedEvent{revealed_to_all=false, revealed_to=controller} (Vision precedent).
- Task 8: fix round 1/5 dispatched — resumed original implementer.
- Task 8: fix round 1/5 (commit 42b10e1; controller re-ran suite: 1105 passed; coverage INCOMPLETE 0; pushed). Minor (a) parked: asserting assault_value==0 after the REAL expiration step needs a new testHook (header edit) — Ruling: deferred; the engine's temp-assault unwind is pre-existing and covered by other cards' tests; cost if wrong: a +2 might leak on Kennen's Assault grant would go unnoticed by the card test alone. Scoped re-review dispatched over 2049853..42b10e1.
- Task 8: re-review round 1: all findings ADDRESSED (Lightning Rush single-choice structurally impossible to double-draw; private look; #28 and #17 now real guards; def-less filter; Flow-card offer tests), no new breakage. Minor (a) stays parked.
- Task 8: complete (commits 5e10eb0..42b10e1, review clean after 1 fix round)
- Task 9: implementer dispatched (opus — last engine surgery: power discount + restricted intent + Tomb card + deck-validator test #26).
- Task 9: implementer DONE (commits 591300f, 4d8bb2a; controller re-ran suite: 1116 passed; coverage 792/INCOMPLETE 0; pushed). Concern: pair-pick spells (Star-Crossed 690, Switcheroo 466 — BOTH in Tyler's deck) get no Tomb discount (pickTargetPair has no restriction filter).
- Task 9: Ruling: under Tyler's fully-faithful ruling the pair-pick case for the two in-scope cards MUST be covered — the friendly half must be at the Tomb to earn the discount; never an unearned discount. Mechanism left to the implementer within the review's fix round. Cost if wrong: Star-Crossed/Switcheroo miss a 1-rune discount the real card grants — a tempo error the self-play would then teach wrongly.
- Task 9: reviewer dispatched (opus) with the pair-pick ruling as a constraint.
- Task 9: reviewer ✅ mechanism / Needs fixes — Important #2: restricted intent's discount granted from the BF flag alone (no friendly-target re-validation; hand-built claim can earn an unearned rune); Important #1: pair-pick gap — reviewer found a caller-agnostic two-branch narrowing in pickTargetPair (publish-A: friendly-at-bf ∪ has-a-friendly-B; publish-B: narrow only if A wasn't friendly-at-bf). Minors: hot-path re-check when no Tomb; guard should always write; trash-replay-grant undiscounted (comment); fizzle-after-discount test; test name; dup loop; file size; OpenSpiel aliasing.
- Task 9: Ruling: implement the reviewer's pair-pick mechanism (in scope under fully-faithful); re-validate the restriction claim like the Flow claim. Parked: OpenSpiel action-id aliasing of restricted vs plain offers (same pre-existing class as the Flow-source aliasing; to be noted in the spec's known limitations); the duplicated emission loop (optional). Cost if wrong: Star-Crossed/Switcheroo discount could be over/under-applied in odd board states — covered by the three mandated tests.
- Task 9: fix round 1/5 dispatched — resumed original implementer.
- Task 9: fix round 1/5 (commit b33a1ad; controller re-ran suite: 1121 passed; pushed). Scoped re-review dispatched over 4d8bb2a..b33a1ad.
- Controller pre-check (not a task): riftbound rebuilt at b33a1ad; seed-7 Kennen(mcts30) vs Rengar(mcts30) → Kennen wins 8 in 12 turns; replay shows Burn 2 → conquer grant → Up from the Deep flowed ×2 and banished; legend empowered/disempowered ×4 (Assault action). Evidence file smoke-precheck-seed7.md in the workspace.
- Task 9: re-review round 1: all findings ADDRESSED (A/B narrowing provably consistent; no unearned discount path; reject precedes all mutation), no new breakage. Open per implementer (parked, pre-existing/out of scope): multi-Tomb asymmetry (one Tomb exists), const_cast RAII in const generators, pickers don't validate the agent's answer against the published list (engine-wide).
- Task 9: complete (commits 42b10e1..b33a1ad, review clean after 1 fix round)
- Task 10: implementer dispatched (sonnet) at HEAD b33a1ad; riftbound binary already rebuilt by the controller; seed-7 pre-check evidence handed over.
- Task 10: implementer DONE (commit 0f993a9; 10 games Kennen 6–4 seat-balanced; zero WRN/assert lines; suite 1121; coverage 792/0). Controller found EMPOWER counted 2× (substring of DISEMPOWER) → docs-only fix round 1 dispatched.
- Task 10: docs fix round 1 (commit 7ac1aea; EMPOWER=28). Task 10: complete (commits b33a1ad..7ac1aea). Final whole-branch review dispatched over 3223848..7ac1aea.
- Controller observation (post-Task 10): Sandswept Tomb was the active battlefield in 2 of 11 smoke games (seeds 2 and 12) and produced 0 restricted plays — consistent with addendum #12: decodeAction returns the FIRST matching legal intent (the plain offer), so the MCTS/OpenSpiel agent can never take the discounted offer while the plain one is affordable. Engine faithful; agent path cannot express it. Follow-up candidate: add flow_source/restriction bits to action_vocab keys.
- FINAL REVIEW (opus, 3223848..7ac1aea): Ready to merge? NO — with fixes. Critical #1: closed-state PlayReaction intents execute via ChainManager::stepExecuteAndPass, NOT executePlaySpell — Flow/Tomb reaction offers (Star-Crossed restricted; Kennen-granted Flow on Flash/Hard Bargain/Star-Crossed) are mispriced, the card is never removed from trash (duplicate trash entry on resolve), banish flag never set, grant never consumed. Important #2: Minefield's Burn 2 skips burn-out (divergent from Kennen's). Important #3: game_object.h is_empowered comment contradicts addendum #10. Important #4: personal-repo references + author identity in committed docs of a third-party fork — Tyler's call. Minors #5–#13 incl. disempower_self claim not re-validated; assert-only null guard; picker validation; two spec-level narrowings (Flow on spells only; tokens to base); cost modifiers bypassed by alt costs; file size; resume_data cast; Hard Bargain pop_back guard.
- FINAL REVIEW Ruling: ONE fix wave — (1) route ChainManager's PlayReaction branch through GameEngine::executePlaySpell via an injected callback (faithful fix, not suppression) + end-to-end closed-state tests for a Flow reaction and a Tomb-restricted reaction; (2) Minefield → burnCards + empty-deck test; (3) header pass in one rebuild: game_object.h comment + intent.h operator== (use_alt_play_cost, granted_ability_def); (4) move PlayedFromFacedownEvent emit into ChainManager's live hidden branch, then delete dead executePlayFromHidden, with a test; (5) fold minors #6 (re-validate disempower_self before paying), #8 (TODO at the three picker sites), #12 (comment), #13 (guard). Deferred: #7 (constructor-init executor), #10 (cost modifiers vs alt costs — pre-existing), #11 (file split). Docs by controller: spec known-limitations (#9), smoke-doc coverage-gap sentence (rec 5). #4 goes to Tyler as a question. Cost if wrong: the callback routing changes a live path — guarded by the full suite + the new end-to-end tests.
- Tyler's ruling (2026-09-07): Important #4 → option (a): fork stays private, docs/superpowers never upstreamed; no stripping. Also: 'institute the fixes for Sandswept Tomb and Flow' — read as (i) the closed-state routing fix already in the fix wave AND (ii) the OpenSpiel action-vocab aliasing follow-up (so the agent can actually take Tomb/granted-Flow offers); (ii) queued as Task 11 after the wave.
- Controller finding (post-final-review): kNumCardDefIds=787 in action_vocab.h while the registry has 792 → the five new cards all fall back to Play slot 0 in the vocab the CLI MCTS agent (mcts_agent.cpp:117/351/448) encodes/decodes through — Kennen, Lightning Rush, Up from the Deep are indistinguishable to the agent. Task 11 brief written (bump + registry guard + distinct Flow/Tomb verbs appended so existing ids stay stable). Dispatch after the fix wave.
- Tyler's ruling (2026-09-07): Task 12 = corpus heuristic evaluator (option 1) AFTER Task 11. NORTH STAR recorded: agents that learn over thousands of games (notes on mistakes, lines, macro/micro plans per opponent deck) and Claude learning from their play to coach Tyler. Roadmap goes in personal-ai vault (self-play-engine.md).
- Tyler's ruling (2026-09-07): the replay-analysis loop is TWO-directional — every lesson Claude extracts must be injected into the agents' priors on the next batch (vault playbooks as single source of truth, injected by a build step, never hand-retyped); proof = agent beats its frozen previous version. Recorded in personal-ai self-play-engine.md.
- FINAL FIX WAVE: DONE (commits 643e7a5..0fc3633; controller re-ran suite: 1129 passed; coverage 792/0; executePlayFromHidden gone; pushed). ONE scoped re-review dispatched over faba6cd..0fc3633.
- FINAL FIX WAVE re-review: all 5 findings ADDRESSED; routing correct and bounded. NEW Important: Hard Bargain (in Tyler's deck) is [Reaction]+[Repeat] and only playable closed-state — its Repeat is live for the first time, but ChainManager's repeat loop (~:398-407) calls resolve_spell directly and cannot pump a pending choice → a paid tranche does nothing or leaks a pending choice to the next card; untested; report's "no in-scope Repeat" claim was wrong. Out-of-scope but same class: resolveShowdownDecision dispatches PlayReaction into executeIntent, which has no case → silent no-op (Rengar, Pouncing is in the Rengar deck). Minor: four comments cite Katarina as 462 (it is 585).
- Ruling: these are load-bearing for both decks and Tyler ruled fully faithful + continue (Tasks 11/12), so the branch is NOT finishing yet → Task 11a before Task 11: (1) make the repeat re-resolution go through the resume pump so a Repeat tranche can consume choices; test Hard Bargain closed-state with Repeat paid on a 3-item chain; (2) route showdown PlayReaction through the same executor (executePlaySpell) with a Pouncing/QuickDraw showdown test; (3) fix the 462→585 comments. Cost if wrong: two more live-path changes guarded by the full suite + new tests.
- Task 11a: implementer DONE (commits e1e34bb..0808d3b; controller re-ran suite: 1135 passed; coverage 792/0; pushed). New out-of-scope finding: closed-state NON-spell reactions (Pouncing/Ambush/Quick-Draw) go through ChainManager's addSpell branch → card pays full cost and lands in TRASH, never on the board (Rengar Pouncing affected). Reviewer dispatched with it as a named risk; ruling: fold into round 1 (route non-spell closed-state reactions through the same executor).
- Task 12 spec + plan written by the controller (docs commit) while Task 11a is in review.
- Task 11a: reviewer ✅ / Approved on substance; Important: three comments assert the now-false "executeIntent has no PlayReaction case"; confirmed from code: closed-state non-spell reactions (Pouncing/Ambush/Quick-Draw) reach addSpell → resolve as a no-op spell → TRASH.
- Task 11a: Ruling: fix round 1 folds the closed-state permanent routing (play_card_ callback → executePlayCard, not a hand-rolled addPermanent) with RED tests (Rengar Pouncing lands at the battlefield; Ambush; Quick-Draw attaches; hidden permanent regression). Cost if wrong: another live-path change, guarded by the suite + new tests; benefit: the Rengar deck's Pouncing finally works, so the Task 11/12 smoke batches face a truthful opponent.
- Task 11a: fix round 1/5 dispatched — resumed original implementer.
- Task 11a: fix round 1/5 (commits 2cf764b, da9a9a2; controller re-ran suite: 1139 passed; coverage 792/0; pushed). Ruling on the flagged judgment: a hidden permanent with no play_location landing at `hidden_at` is the CR 811 locus — KEEP (cost if wrong: one line). Bare-ChainManager fallback kept for spells only (two engine-less tests depend on it); permanents without an executor are rejected loudly. Scoped re-review dispatched over 0808d3b..da9a9a2.
- Task 11a: scoped re-review dispatched (opus). Task 11 implementer dispatched IN PARALLEL (disjoint files: src/openspiel/action_vocab.*, tests/test_action_vocab.cpp) — reviewers are read-only so no tree conflict.
- Task 11a: re-review round 1: Findings 1–3 ADDRESSED; routing correct on all four named checks (executor map complete; cost paid once; fallback unreachable for permanents). Findings remain open: ONE stale comment clause at game_engine.cpp:~1128-1129 ("non-spells take the chain's own local branch") + minor: chain_manager.cpp:~458 `*resuming` vs the has_value guard at :449 (keep .value() or drop the guard). Round 2 = comment + one-line consistency; HELD until Task 11's implementer finishes (no parallel ninja builds in one build dir).
- Task 11a: observations parked: runChain re-entrancy comment names only executePlaySpell; closed-state Quick-Draw lands at base then attaches (pre-existing); CR 355.2.a base-or-battlefield choice for revealed permanents not offered (generator-side); hidden block ignores bf.blocks_unit_play (pre-existing).
- Task 11: implementer DONE (commit c22a35a; controller re-ran suite: 1143 passed; pushed). Smoke inconclusive (no success-path trace for a chosen restricted play). Flagged: src/ml/feature_extractor.h kCardVocabSize=787 stale (ML side, out of scope). Reviewer dispatched; Task 11a round 2 dispatched (files disjoint).
- Task 11: reviewer ✅ / Approved (kVocabSize 9615→12051, arithmetic verified; precedence documented in three places; id stability proven). Important-for-later (parked, ML side out of scope): src/ml/feature_extractor.h kCardVocabSize=787 silently drops cards 788–792 from observation-tensor zone multi-hots — add a registry guard like Task 11's when the ML pipeline is next touched. Minor: PlayTombRestricted collapses on card slot (one Tomb per game; documented).
- Task 11: complete (commit c22a35a, review clean)
- Task 11a: fix round 2/5 (commit c561d2e; comments + .value(); suite 1143; pushed). Scoped re-review (haiku) dispatched. Task 12 implementer dispatched in parallel (disjoint files).
- Task 11a: re-review round 2: both findings ADDRESSED, no breakage.
- Task 11a: complete (commits 0fc3633..c561d2e, review clean after 2 fix rounds). Parked/deferred from 11a: CR 355.2.a base-or-battlefield choice for revealed permanents (generator-side); hidden block ignores bf.blocks_unit_play; closed-state Quick-Draw lands at base then attaches (pre-existing).
- Task 12: implementer dispatched (opus) at HEAD c561d2e with spec + plan; its A/B batches run in background logs.
- CONTAINER RESTART ~13:10 UTC (2026-09-07): all background agents/processes killed; filesystem SURVIVED (clone, build, ledger). Task 12 implementer lost mid-12.3. Its commits d25584c (12.1) and 90f39c0 (12.2) survived locally and are now pushed to the fork; suite 1196 passed (controller re-ran); coverage 792/0. 12.3 partial: threads=4 batches gave score-vs-score Kennen 85/200 (P1 47, P2 38), corpus-Kennen P1 45/100, corpus mirror 92/200 — NOT distinguishable; AND a crash: eval=corpus with --threads>1 aborts/segfaults ("Battlefield not found: <garbage>"), clean at threads=1 → memory corruption under concurrency (hypothesis: latent race in the batch/agent layer that the score evaluator's two-int read survives). Controller restarted the four corpus batches as four single-threaded PROCESSES (abst-*.log, seed 1000, 100 games each).
- Crash root cause (investigator, read-only): PRE-EXISTING use-after-free — GameState::battlefields is a std::vector at size==capacity==2 after setup; Baron Nashor's addBattlefieldToken (effect_executor.cpp:~853) reallocates it while three engine sites hold BattlefieldState& across card resolution (game_engine.cpp:~857-874 staged-BF loop, runShowdown ~:3799, runCombat ~:3963) → stale id read → "Battlefield not found: <garbage>". Score evaluator merely survives (no allocation between free and reuse; fewer battlefield fights); threads change allocator arenas. Confidence high the defect is real; ~70% it is THE abort. Ruling: FIX after the batches finish (CPU) — Task 12a: switch battlefields to std::deque (stable references on push_back) AND re-fetch by id at the three sites; tests: reference-stability unit test (fails with vector), a threads=2 20-game batch in the smoke doc; ASan build in a separate dir as confirmation if time allows. Then re-run the corpus A/B (single-threaded results may be silently affected in Baron-Pit games).
- Task 12.3 (controller-run, single-threaded processes, seed 1000, 100 games/cell, sims 20, zero crashes): baseline score-vs-score Kennen 85/200 (P1 47, P2 38); treatment corpus-Kennen 80/200 (P1 45, P2 35); MIRROR corpus 92/200 (P1 47, P2 45) → Wilson 95% 39.2–52.9% includes 50% → NOT DISTINGUISHABLE (point estimate slightly below). Cells that also completed at threads=4 before the crash gave IDENTICAL numbers (determinism confirmed). Task 12a (UAF fix) dispatched; then the mirror control re-runs on the fixed engine.
