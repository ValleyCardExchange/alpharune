# SDD ledger — plan: docs/superpowers/plans/2026-09-07-replay-loop-iter0.md

Spec: docs/superpowers/specs/2026-09-07-replay-loop-iter0-design.md. Branch replay-loop off master 8006d38 (fork ValleyCardExchange/alpharune). Tyler approved "Full Iteration 0" 2026-09-07 after "Merge it and start the replay analysis loop."
Pre-flight: L1 (C++ writer) and L2 (Python script) dispatched IN PARALLEL on disjoint files; L2 writes to the spec's schema and is validated on L1's real output by the controller before L3. L3/L5 are controller-run (batches + Claude's playbook). L4 after L3.
## Task log
- L1 dispatched (sonnet); L2 dispatched (sonnet).
- L2: DONE (commit 818ef91; controller re-ran unittest: see above). Aligned parsing to the real L1 writer shape (log[] not decisions[], top-level footer, phase.phase/oc_state, p1/p2 unit keys, dict destination) — verify on real logs before L3.
- L1: DONE (commit f657e57; controller re-ran suite: 1202 passed; identity check reproduced; pushed). Ruling on the size bar: 325–507 KB/game is the true decision count (~400–620 decisions/game), not bloat — target raised to 600 KB; logs are never committed (summaries only). Controller ran L2's script on two real games: parses, all sections populated, Flow printed / closed-state reactions / conquers counted; burns/empowers omitted (no marker in schema) — acceptable for iteration 0.
- L1+L2 joint review dispatched (sonnet). L3 batches STARTED by the controller in parallel (Kennen P1 seed 2000; Kennen P2 seed 3000; 200 games each; sims 20; threads 4; logs outside the repo at /home/user/chorlick/batches/iter0/).
- L4 dispatched (sonnet) in parallel with the L3 batches (batches use the already-built binary; L4's relink does not disturb the running process).
- HALT: L1+L2 review Approved but flagged a real engine anomaly: closed-state ActivateReactionAbility (Seal of Discord x3 — Kennen's power engine) appears as 10–93-decision bursts with frozen state. Controller confirmed chain_manager.cpp:~352 leaves ActivateReactionAbility unhandled in the closed state → returns false → silent no-op (offered, never executed, never exhausted). Ruling: the running L3 batch is INVALID for Kennen — killed; fix test-first (route closed-state ability activations through executeIntent via a callback, like play_spell_/play_card_) and re-run L3. Same 'second executor' family as addendum #14.
- L4: DONE per agent (commit ec2a706; claims 1235/1 disabled, +33 tests; default-valued prior file reproduces the no-prior game at seed 777). Controller check pending: suite re-run deferred until the ability-fix agent releases the build dir; commit contents verified to contain ONLY L4 files (the in-flight chain_manager/game_engine edits are still uncommitted). Ruling: caps and the unclassified-intent fallback stay out of schema v1 (matches the spec's key list).
- Ability fix: DONE (commit 8b26d29; controller re-ran: suite 1240/1 disabled, gate 0 incomplete). Agent found and fixed the showdown half too (executeIntent's activation case lacked ActivateReactionAbility). Ruling: CR 429.2 immediate-resolution for [Add] abilities stays unmodelled (whole class, separate ruling); the callback's fingerprint-based "did it execute" is accepted for now (a status code needs a game_engine.h edit).
- HALT #2 (controller smoke, 4 games seed 2000 on the fixed binary): 3/4 games clean (13–21 activations, no bursts); game 0 shows 497 repeated `ActivateAbility (src=Last Rites) tgt=[Traveling Merchant]` — the EQUIP offer. Root cause: generateMainPhaseActions' equip block offers every unattached friendly gear with NO affordability check, and executeIntent drops onEquip()==false silently. kMainPhaseMaxActions=500 means each burst consumed Kennen's WHOLE turn (turns 4,6,10,12,14 in game 0). Card-level faithfulness bugs on the same path: Last Rites recycles 2 trash cards BEFORE the power check (partial payment possible when a Chaos rune exists but is exhausted); card_helpers' payOnePower requires a READY rune, contradicting CR 164.2.b (Recycle this: Add [C] — no readiness condition) and the engine's own canonical payer (game_engine.cpp ~5509). Ruling: fix generically (Card::canEquip legality predicate + generator gate + loud warn) with per-gear predicates mirroring each onEquip pre-check, all-or-nothing by construction; guard test over the registry. Every prior batch (corpus A/B, 24-game pool) is contaminated for Kennen whenever Last Rites sat unequipped — noted for the smoke docs.
- Spec addenda #15/#16 + plan task L0b committed (b406465). L0b implementer dispatched (opus, TDD, owns the build dir). Read-only reviewer dispatched (sonnet) on review-f657e57..8b26d29.diff (L4 + ability fix). replay-loop pushed to fork through b406465.
- L2b (controller, while L0b runs): analyze_batch.py Validity section — frozen-state burst detector (run >= 10 identical full-state decisions). RED then GREEN (28 tests); on the seed-2000 smoke logs it flags the 497-run in game 0 only. Committed + pushed. Ruling: any batch whose summary shows a burst is INVALID for the affected deck, by rule, not by judgment.
- Review of ec2a706 + 8b26d29 (sonnet, read-only): APPROVED WITH NOTES (.superpowers/sdd/2026-09-07-replay-loop/review-l4-abilityfix.md). (1) Moderate but unreachable today: the setActivateAbility callback infers "did it execute" from a resource fingerprint that ignores the opponent's state, board state, and rune-object exhaustion; a future zero-exhaust energy-only [Reaction] ability with an opponent-only effect and no chain item would be misreported as rejected, leaving passes unreset (CR 337.1.b.3). Every activation in today's registry goes on the chain, so chain-size catches all real cases. (2) Low, pre-existing: executeIntent's activation energy loop never checks the cost was FULLY paid — an underpaid hand-built intent still executes. (3) Low: loadPriorConfig throws nlohmann type_error for non-numeric values, not the documented runtime_error. Ruling: queue all three as task L0c AFTER L0b lands (game_engine.h status return for the activation path; `needed > 0` reject; rethrow as runtime_error) — no build-dir contention with the running implementer; none blocks the L3 batch because none is reachable by the generators' offers.
- L0b: DONE (commit b1f2a89). Controller re-ran: suite 1246/1 disabled (1240 + 6), gate INCOMPLETE 0, tree clean; 8-game smoke seed 2000..2007 on the fixed binary: analyzer Validity = OK, longest identical run 2 (was 497), 4–4 split, avg decisions 322 (was 425 with the bursts), no EQUIP-rejected warnings. Guard test enumerated 36 equip gear from the registry and caught a THIRD bug on the way: UniversalEquipGear (504/507/509) attached with an empty base (paid nothing). Rulings recorded: Hextech Gauntlets' target-dependent energy is target-agnostic in canEquip (cheapest target) — residual offer→warn possible, not a burst since the warning is loud and the offer is bounded per target; Blade of the Ruined King's "kill another friendly unit" rendered exactly as >=2 friendly units. Pushed. L3 batches STARTED on b1f2a89 (script: scratchpad/run_l3.sh; Kennen P1 seed 2000 / Kennen P2 seed 3000; 200 games each; sims 20; threads 4; logs /home/user/chorlick/batches/iter0/). Reviewer dispatched on review-b406465..b1f2a89.diff (cae7532 + b1f2a89).
- Review of cae7532 + b1f2a89 (sonnet): CHANGES REQUIRED (.superpowers/sdd/2026-09-07-replay-loop/review-l0b.md): (1) HIGH one rune can pay both energy and power on the equip path (pre-existing in standardEquip, now codified by the predicate; canonical payAdditionalCost excludes the recycled rune); (2) HIGH target-dependent cost (Hextech Gauntlets) still offered per target and rejected repeatedly — loud burst; (3) MEDIUM Blade of the Ruined King kills before paying; (4) LOW fingerprint lacks priority depth — threshold 10 kept, framing softened in docs. RULING: batch decks contain exactly one equip gear (Last Rites, energy 0), so none of (1)–(3) is reachable — the running iteration-0 batch STAYS VALID; all three plus the three #15-review items folded into spec addendum #17 / plan task L0c (committed). P1 seat complete: 200 games, Validity OK, Kennen 96/200 = 48.0% (Wilson 41.2–54.9). P2 seat running on a COPIED binary (scratchpad/riftbound-b1f2a89) so L0c may rebuild ./build without touching the batch.
- L3: DONE. Both seats complete on the copied b1f2a89 binary; Validity OK both; Kennen 186/400 = 46.5% [41.7–51.4] (P1 96/200, P2 90/200). Batch record committed under docs/superpowers/smoke/2026-09-08-loop-iter0/ (summaries + deep read + script). Playbook + priors written in personal-ai (playbooks/kennen-vs-rengar.md / .prior.json, commits df56086 + 36e0e8e, pushed). Priors change ONLY move_to_base 0.6→1.2 and move_to_battlefield 3.0→3.5 (evaluator weights set but inert with eval=score). L5 STARTED (scratchpad/run_l5.sh; frozen binary; seeds 2000/3000; 200/seat; logs /home/user/chorlick/batches/iter0-l5/). Accept rule: new interval's lower bound > frozen point estimate (48.0 / 45.0 / 46.5).
- L5: DONE. Prior-injected Kennen 100/200 (P1) + 93/200 (P2) = 193/400 = 48.2% [43.4–53.1] vs frozen 46.5% → REJECT / not confirmed (pooled lower bound 43.4 < 46.5). Validity OK both seats. Behaviour shift small: zero-retreat games 51→35, ≥5 empty-board turns 142→122, early board unchanged; within-batch correlations replicate. Recorded in docs/superpowers/smoke/2026-09-08-loop-iter0/acceptance.md (+ L5 summaries) and pushed. Ruling for iteration 1 recorded (more sims / evaluator lever with eval=corpus / larger n) — Tyler chooses.
- L0c: DONE (commit 192e9e1). Controller re-ran: suite 1255/1 disabled (1246 + 9), gate INCOMPLETE 0, tree clean; 8-game smoke seeds 2000..2007 on the L0c binary reproduces the b1f2a89 games EXACTLY (same winners, turns, decisions) → behaviour-neutral for the batch decks, as ruled. Implementer's rule readings recorded: (a) Blade "drain between check and pay" is unreachable through re-entry (onEquip restarts from the top); the reachable window is inside killObject, tested via a UnitDied handler; (b) activation energy pre-check spends the floating pool first, mirroring payCardCost/payAdditionalCost. Concerns queued for a later ruling: ActivationCost::power never paid by executeIntent (pre-existing, unused); payEquipCost doesn't clear is_exhausted on the recycled rune (pre-existing, all equip payers); no registry guard for canEquipTarget overrides. Pushed. Reviewer (sonnet) dispatched on review-591967a..192e9e1.diff.
- Review of 192e9e1 (sonnet): APPROVED WITH NOTES. Two new pre-existing holes confirmed: activation power costs never gated/paid (5 shipped cards, none in the batch decks); recycled exhausted runes keep is_exhausted (equip payers only; canonical payer clears it). Ruling: spec addendum #18 + plan task L0d QUEUED — Tyler chooses when; batch record notes the Last Rites bias. Iteration 0 CLOSED: L1–L5 done, verdict REJECT/not confirmed, three engine holes fixed (#15, #16, #17) and one queued (#18). Ledger archived here at close.
